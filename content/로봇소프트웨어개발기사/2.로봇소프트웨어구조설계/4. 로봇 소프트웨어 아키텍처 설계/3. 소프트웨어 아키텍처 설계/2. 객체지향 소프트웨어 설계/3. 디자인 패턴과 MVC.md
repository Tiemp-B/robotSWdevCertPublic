---
cssclasses: cornell-note
tags: [로봇소프트웨어개발기사, 로봇소프트웨어구조설계, 로봇소프트웨어아키텍처설계]
과목: 로봇소프트웨어구조설계
주요항목: 4. 로봇 소프트웨어 아키텍처 설계
세부항목: 3. 소프트웨어 아키텍처 설계
세세항목: 2. 객체지향 소프트웨어 설계
---

# Summary

디자인 패턴은 반복되는 설계 문제에 대한 검증된 해법이며, **[[싱글톤 패턴(Singleton Pattern)|싱글톤]]** ·**[[옵저버 패턴(Observer Pattern)|옵저버]]** 패턴과 UI 구조화에 널리 쓰이는 **[[MVC 패턴(Model-View-Controller)|MVC 패턴]]**이 로봇 소프트웨어에서도 자주 쓰인다.

---

<div class="cues-header">Cues</div>

# Notes

<aside>싱글톤(Singleton) 패턴</aside>

싱글톤 패턴은 클래스의 **인스턴스가 프로그램 전체에서 오직 하나만 생성**되도록 보장하고, 어디서든 그 유일한 인스턴스에 접근할 수 있는 전역 접근점을 제공하는 패턴이다.

<aside>옵저버(Observer) 패턴</aside>

옵저버 패턴은 한 객체(주체, Subject)의 상태가 변할 때, 그 객체를 구독(관찰)하고 있는 여러 객체(옵저버)들에게 자동으로 변경 사실을 통지하는 패턴이다. [[3. 통신 모델(발행-구독·요청-응답·액션)|발행-구독(Publish-Subscribe)]] 통신 패턴과 개념적으로 유사하다.

- 로봇 소프트웨어에서의 용도: 센서 값이 갱신될 때마다 이를 구독하는 여러 모듈(로깅, UI 표시, 제어 로직)에 자동으로 알림

<aside>전략(Strategy) 패턴</aside>

**[[전략 패턴(Strategy Pattern)|전략 패턴]]** 은 여러 알고리즘을 각각 별도 클래스로 캡슐화해, 런타임에 서로 바꿔 끼울 수 있게 하는 패턴이다. 로봇의 경로 탐색 방식을 "장애물 회피 우선"·"최단 거리 우선"처럼 별도 클래스로 만들어 두고, 상황에 맞는 전략을 클라이언트(외부 코드)가 골라 주입하는 식으로 활용한다. 객체가 스스로 내부 상태에 따라 행동을 전환하는 [[2. 상태 기계와 예외 처리|상태 기계]]와는 달리, 전략 패턴은 외부에서 알고리즘을 선택·주입한다는 점이 다르다.

<aside>MVC(Model-View-Controller) 패턴</aside>

MVC는 애플리케이션을 세 가지 역할로 분리하는 아키텍처 패턴이다.

| 구성 요소 | 역할 |
|---|---|
| Model | 데이터와 비즈니스 로직(예: 로봇의 현재 상태·센서 데이터) |
| View | 사용자에게 보여지는 화면(예: 로봇 상태 대시보드 UI) |
| **Controller** | **사용자 입력을 받아 Model을 갱신하거나 View의 표시 방식을 제어** |

<aside>예시 — 로봇 원격 제어 UI에 MVC와 옵저버 적용</aside>

**예시**: 사용자가 원격 제어 화면에서 "정지" 버튼을 누르면(Controller가 입력을 받아 처리) 로봇의 상태(Model)가 "정지"로 갱신되고, Model의 상태 변경을 구독하고 있던 옵저버(View, 로그 기록 모듈 등)들이 자동으로 통지를 받아 화면 표시와 로그를 갱신한다.

---

<aside>핵심 정리</aside>

- 싱글톤은 인스턴스를 오직 하나만 생성·공유하는 패턴이다
- 옵저버는 주체의 상태 변화를 구독자들에게 자동 통지하는 패턴이다
- 전략 패턴은 여러 알고리즘을 캡슐화해 외부에서 선택·주입하는 패턴이며, 객체 스스로 상태에 따라 전환하는 상태 기계와는 다르다
- MVC의 Controller는 화면 렌더링이 아니라 사용자 입력 처리와 Model 갱신을 담당한다

---

<aside>관련 노트</aside>

- 개요(허브): [[1. 객체지향 소프트웨어 설계 개요]]
- 관련: [[2. 캡슐화와 SOLID 원칙]]
- 관련: [[3. 통신 모델(발행-구독·요청-응답·액션)]] — 발행-구독 패턴(1과목)
- 용어: [[싱글톤 패턴(Singleton Pattern)]], [[옵저버 패턴(Observer Pattern)]], [[MVC 패턴(Model-View-Controller)]], [[전략 패턴(Strategy Pattern)]]
